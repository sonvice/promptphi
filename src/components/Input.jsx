import { ChakraBaseProvider, Input } from '@chakra-ui/react';

function Form() {
  return (


      <Input variant='outline' width='auto' placeholder="Ingresa tu email" size="lg" />
  );
};

export default Form;
